# Copy to .env (gitignored) and source before running.
# Or use `substrate config init` to write a persistent profile instead.

SUBSTRATE_MCP_TOKEN=mcp_replace_me
SUBSTRATE_API_BASE_URL=https://your-supabase-project.functions.supabase.co/ondemand-mcp-manager
# SUBSTRATE_PROFILE=default

# For workloads that pull from Hugging Face:
# HF_TOKEN=hf_replace_me
