# Copy to .env (gitignored) and source before running.
# Or use `substratecloud config init` to write a persistent profile instead.

SUBSTRATECLOUD_MCP_TOKEN=mcp_replace_me
SUBSTRATECLOUD_API_BASE_URL=https://your-supabase-project.functions.supabase.co/ondemand-mcp-manager
# SUBSTRATECLOUD_PROFILE=default

# For workloads that pull from Hugging Face:
# HF_TOKEN=hf_replace_me
